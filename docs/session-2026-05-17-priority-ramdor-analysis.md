# הערות מסשן ניתוח דוחות פריוריטי + רמדור (2026-05-17)

> מסמך זה מסכם **את המסקנות** מהסשן שבו ניתחנו את הקבצים הבאים של פרויקט 2253.
> הקבצים עצמם **לא נשמרו** (קונטיינר זמני) — צריך להעלות אותם שוב בסשן הבא.
> **לא מכיל נתונים אמיתיים** — רק מבנים, סכומים מסכמים, וסטטיסטיקות.

---

## הקבצים שניתחנו בסשן

### קובץ 1: דוח תזרים מפריוריטי (`8be125bd-...0__2253__20042026.xlsx`)
- **מקור**: ייצוא ישיר מפריוריטי, בלי ניקוי ידני של המשתמש.
- **תוכן**: 1 גיליון (`תזרים_3_26`), 638 שורות, 23 עמודות.
- **כולל**: כל ההוצאות וההכנסות של הפרויקט.

### קובץ 2: דוח הזמנות רכש מרמדור (`feb88740-...`)
- **מקור**: מערכת רמדור (מערכת רכש נפרדת מפריוריטי).
- **תוכן**: 1 גיליון (`DataSheet`), 397 שורות, 32 עמודות.
- **כולל**: 108 הזמנות רכש ייחודיות, 396 פריטים.

---

## מבנה דוח התזרים (פריוריטי)

### 23 עמודות
```
0-2:  IDs פנימיים (להתעלם)
3-4:  מרכז רווח/עלות (2253) + תאור
5-6:  חשבון + תאור חשבון (קטגוריה!)
7-8:  ת. מאזן + ת. ערך (תאריכי רישום)
9:    פרטים (תיאור הפעולה)
10-11:חובה + זכות (סכומים בש"ח)
12-13:אסמכתא + אסמכתא 2 (מספרי חשבונית)
14-15:חשבון נגדי + תאור (שם הספק!)
16:   מס. תנועה (ID יחיד)
17-18:סעיף מאזן בוחן + כותרת
19-20:פרויקט + תאור פרויקט
21-22:סעיף תקציבי + תאור
```

### חלוקת תנועות לפי סוג חשבון (col 7 - תאור חשבון)
| כמות | סוג חשבון | מיפוי שלנו |
|---|---|---|
| 92 | הוצ' שכ"ע עלות המכר | תקורות → שכר |
| 91 | הוצ' רכב - עלות המכר | תקורות → רכב |
| 89 | חומרים | חומרים |
| 32 | קבלני משנה | קבלני משנה |
| 23 | אחזקת כלי צמ"ה | תקורות → ציוד |
| 10 | יבוא | חומרים |
| 10 | ארוחות עובדים | תקורות → סיבוס |
| 8 | הכנסות מביטוח לאומי-מילואים | קיזוז משכר |
| 7 | הכנסות מפרויקטים ממזמין | **הכנסות (7 חשבונות חלקיים)** |
| 7 | שונות הוצ' כלליות | תקורות → שונות |
| 5 | הוצאות לינת עובדים | תקורות → לינה |
| 1 | ביטוח יבוא | חומרים |
| 1 | השכרות ציוד | תקורות → ציוד |

### עובדים שזוהו (מהפרטים של שורות שכ"ע)
- עובד 1511
- עובד 2385
- עובד 9593
- עובד 6197

(שכר מפורט פר עובד פר חודש — אפשר לקשור לעובד אוטומטית.)

### בעיה מזוהה: לא ניתן לקשר דלק/ליסינג/סיבוס לעובד מהקובץ הזה
- שורות דלק: "סונול 1.25" = סך החודש לכל העובדים מ-סונול.
- שורות ליסינג: "ליסינג 3.25" = סך החודש לכל הרכבים מאלבר.
- שורות סיבוס: "סיבוס 12.2024" = סך החודש.
- **אין מספר כרטיס/רכב/עובד בשורה.**

**פתרון**: לקבל דוחות נפרדים מאלבר/סונול/סיבוס שמפרטים פר כרטיס/רכב/נהג.
או: לבנות באפליקציה כללי הקצאה ידניים (% לעובד).

### ביטולים
- 11 שורות עם "ביטול ל [מספר תנועה]" — סכומים שליליים.
- מבטלות רישומים קודמים שגויים.
- **החלטה (Q1)**: שתי השורות נשמרות (חיוב + קיזוז שניהם נראים).

---

## מבנה דוח רמדור (PO)

### 32 עמודות עיקריות
```
ת. הזמנה | הזמנה (PO#) | מס' ספק | מק"ט | תאור מוצר | כמות | סגורה? |
יתרה לאספקה | יחידה | שווי יתרה | מטבע (USD/EUR/ש'ח) | יתרה מפעל |
תאריך אספקה | תאריך כניסה למלאי | כמות מפעל | תאריך אספקה מבוקש |
ת. תשלום | מאושרת? | החותם הבא | יצרן | סטטוס שורת הזמנה |
מנוהל מלאי | חברה (לסיכו) | פרויקט | שם ספק |
מחיר ליחידה | מרכז רווח/עלות | סה"כ מחיר | סעיף תקציבי |
תאור סטטוס הזמנה
```

### חלוקת PO לפי סטטוס

| סטטוס | שורות | סכום | משמעות |
|---|---|---|---|
| סגורה | 219 | ₪2,878,497 | סופק במלואו + חשבונית התקבלה |
| סגירה חלקית | 73 | ₪105,940 | חלק סופק/חויב, חלק עוד בהמתנה |
| מבוטלת | 70 | ₪108,937 | בוטלה |
| **נשלחה** | 29 | **₪267,138** | **סופק אך טרם נסגר — חשבונית כנראה לא הגיעה** |
| אושרה | 3 | ₪15,450 | אושרה — טרם נשלחה לספק |
| טיוטא | 2 | ₪1,950 | טיוטא — לא פעילה |

### 18 PO לא סגורות (לא סגורה, לא מבוטלת)
- סך לא סגור: **₪390,478**
- יתרה לאספקה: **₪271,781**
- **PO הכי גדולה פתוחה**: ₪240,655, סטטוס "נשלחה" (חצי שנה במצב הזה)

### בעיה מזוהה: התאמה PO ↔ חשבונית קשה
- בקובץ תזרים, מספר ה-PO **לא נכתב** בשורת החשבונית.
- ההתאמה תיעשה אוטומטית באפליקציה דרך **fuzzy match**: ספק + סכום (±10%) + תאריך קרוב.

---

## החלטות שהתקבלו בסשן

### תת-קטגוריות מעבר ל"תקורות"
המשתמש רוצה ניתוח **עלות פר עובד** — שכר + דלק + סיבוס + ליסינג + רכב לכל אחד.
דורש אוסף חדש: `employees/` + מיפוי רכבים/כרטיסי דלק/סיבוס לעובדים.

### קיזוזים מקבלן
המשתמש לפעמים משלם משהו עבור קבלן ואז מקזז מהחשבון שלו.
- שדה חדש: `expense.linkedContractorOffset` → ref ל-`contractors/`.
- שדה חדש: `contractor.totalOffsetDeductions` → מחושב.
- שתי השורות נשמרות בנפרד (גם החיוב גם הקיזוז) — לא ממוזגות.

### ניהול PO (סטטוס מותאם)
המשתמש מבקש שכבת ניהול ידנית מעל סטטוס Ramdor.
**8 סטטוסים מוצעים** (טרם אושרו סופית):
```
pending / verified_closed / awaiting_invoice / cancelled_unused /
partial_complete / invoice_pending_approval / invoice_rejected /
needs_review
```
+ שדה `userStatusReason` (free text)
+ `manuallyLinkedInvoiceIds` (קישור ידני)
+ `statusHistory` (audit trail)

### התראות חכמות בדשבורד (טרם נסגרו)
דוגמאות שהוצעו:
- PO ב-"נשלחה" יותר מ-30 יום בלי חשבונית
- PO ב-"סגירה חלקית" יותר מ-60 יום
- חשבונית שלא שולמה והגיעה לתאריך פירעון

---

## מה עוד חסר לסיום מלא של המבנה

### חובה לסשנים הבאים
1. **דוח כרטסת ספק** מפריוריטי (פר ספק בפרויקט 2253)
   - יוסיף: תאריכי פירעון, סטטוס תשלום, יתרות פתוחות.
2. **דוח חשבוניות לקוח / יתרות לקוחות** מפריוריטי
   - יוסיף: סטטוס תשלום של 7 החשבונות החלקיים.

### מומלץ
3. **PDF של החוזה הראשי** (מטושטש)
   - יוסיף: תקציב מקורי, תנאי תשלום, ערבויות, מדדים, סנקציות, מסירה.
   - יאפשר AI על תוכן החוזה.

### אופציונלי
4. דוחות שכר מפורטים (שכר ברוטו/סוציאליות/מס')
5. דוחות מ-אלבר/סונול/סיבוס פר כרטיס
6. אותם דוחות לפרויקטים 2288 ו-2306
7. PDF של תכניות AutoCAD (קישור בלבד)

---

## מה לעשות בסשן הבא

### שאלות שעוד פתוחות מהסשן הזה
1. אישור 8 הסטטוסים המוצעים ל-PO (או הוספה/שינוי).
2. שדה "סיבה" — חובה או אופציונלי?
3. זמני המתנה להתראות (30 / 60 / 90 יום).
4. השמטת `cancelled_unused` מדוחות פיננסיים — כן/לא?

### זרימת עבודה מוצעת לסשן הבא
1. אם יש: לקבל את הקבצים החסרים (כרטסת + חשבוניות לקוח + חוזה).
2. אם אין: להתחיל לבנות importer רק עם תזרים + PO.
3. בכל מקרה: **לבנות את ה-importer בקוד** ולייבא ל-Firestore.
4. אחרי הייבוא: דשבורד אמיתי מציג נתונים אמיתיים של 2253.

### לפני הסשן הבא — המשתמש צריך
- להעלות שוב את הקבצים שאיבדנו בסיום הסשן הזה:
  - `priority-cashflow-2253.xlsx` (8be125bd)
  - `ramdor-po-2253.xlsx` (feb88740)
  - הקובץ הנקי שלו `2253 R02.xlsx`
- לנסות להוציא מפריוריטי דוח כרטסת ספקים (F11 → "כרטסת ספק").
- לנסות להוציא דוח חשבוניות לקוח.
- (אופציונלי) להעלות PDF חוזה מטושטש.

---

## עדכונים שצריך לעשות ב-PLAN.md בסשן הבא

על סמך מה שלמדנו, נצטרך לעדכן את PLAN ל-**גרסה 3.5**:

1. **אוסף `employees/`** מלא עם המיפויים.
2. **שדה `subCategory`** ב-`expenses/`.
3. **שדה `linkedContractorOffset`** ב-`expenses/`.
4. **אוסף `purchase_orders/`** מתעדכן עם userStatus, statusHistory.
5. **שדה `paymentStatus` + `dueDate` + `paidDate`** ב-`expenses/` (כשנקבל את כרטסת הספק).
6. **שדה `status` (paid/partial/pending) + `paidDate`** ב-`income/` (כשנקבל יתרות לקוחות).
7. הוספת **`exchange_rates/`** (קיים ב-PLAN 3.4 כבר).
8. עקרון: **כל קטגוריה = free text + רשימת suggestions**.
